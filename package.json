{
  "name": "clevia",
  "version": "1.0.0",
  "type": "module",
  "description": "clevia — App + Landing + API (ein Repo, containerisiert)",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "express": "^4.22.2",
    "resend": "^4.0.0",
    "stripe": "^17.0.0",
    "ws": "^8.18.0",
    "pdf-parse": "^1.1.1"
  },
  "optionalDependencies": {
    "@sentry/node": "^8.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}